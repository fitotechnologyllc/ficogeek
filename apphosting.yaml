# Configuration for Firebase App Hosting
# This minimal configuration avoids 'preparer status 100' errors.
# Build-time environment variables are no longer required due to 'force-dynamic' routes.

runConfig:
  nodeVersion: 20
